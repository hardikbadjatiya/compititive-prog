
import java.util.Scanner;

public class Train {
public static void main(String[] args) {
   Scanner sc=new Scanner (System.in);
   int r=sc.nextInt();
   for(int i=0;i<r;i++)
   {
       int n=sc.nextInt();
       int a=sc.nextInt();
       int b=sc.nextInt();
       int c=sc.nextInt();
       int d=sc.nextInt();
       int p=sc.nextInt();
       int q=sc.nextInt();
       int y=sc.nextInt();
       
       for(int j=0;j<n;j++)
       {
       int x=sc.nextInt();
       }
       int h=Math.max(a, b)-Math.min(a, b);
       if(Math.max(c,d)>=Math.max(a, b)&&Math.min(c,d)<=Math.max(a, b))
       {
           
       }
   }
}
    
}
